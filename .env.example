# -----------------------------------------------------------------------
# Discord — Bot API (requerido para botones interactivos de backfill)
# -----------------------------------------------------------------------
# Token del bot (Discord Developer Portal → Bot → Token)
DISCORD_BOT_TOKEN=YOUR_DISCORD_BOT_TOKEN

# ID del canal donde se envían las alertas (click derecho en el canal → Copy Channel ID)
DISCORD_CHANNEL_ID=

# ID de la aplicación (Discord Developer Portal → General Information → Application ID)
DISCORD_APPLICATION_ID=

# Public key para verificar firmas de interacciones (General Information → Public Key)
DISCORD_PUBLIC_KEY=

# -----------------------------------------------------------------------
# Discord — Webhook (fallback sin botones, retrocompatible)
# -----------------------------------------------------------------------
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# -----------------------------------------------------------------------
# Supabase
# -----------------------------------------------------------------------
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Base URL para re-ejecutar edge functions en el reintento de backfill
# Formato: https://YOUR_PROJECT_REF.supabase.co/functions/v1
FUNCTIONS_BASE_URL=https://YOUR_PROJECT_REF.supabase.co/functions/v1

# JWT anon key o service role key usada para invocar edge functions en retries
# (la misma que usarías en el header Authorization: Bearer <key>)
FUNCTION_INVOKE_KEY=YOUR_ANON_OR_SERVICE_ROLE_KEY

# -----------------------------------------------------------------------
# Configuración general
# -----------------------------------------------------------------------
PROJECT_NAME=supabase-monitor-bot

# Override opcional del nombre de la tabla (por defecto: function_errors)
ERROR_LOG_TABLE=function_errors
